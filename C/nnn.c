#include <stdio.h>
 
int main()
{
    int n,j=0,k,m,i;
    printf("Enter a number:");
    scanf("%d",&n);
    m=itoa(n);   
    
    while(n>0)
    {
 
        if(m(i)!=0&&1)
        {
           printf("The given number is not binary");
           break;
        }
        i=i+1;
 
        if(n==0)
        {
            printf("The given number  is binary");
        }
    }
    return 0;
}